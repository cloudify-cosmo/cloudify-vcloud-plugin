[tox]
envlist = nosetest,pep8
minversion = 1.6
skipsdist = True

[testenv]
setenv =
        VIRTUAL_ENV={envdir}

# NOTE: relative paths were used due to '-w' flag for nosetests util

usedevelop = True
install_command = pip install -U {opts} {packages}
deps = -r{toxinidir}/dev-requirements.txt
       -r{toxinidir}/test-requirements.txt
whitelist_externals = bash

[testenv:code_quality]
commands =
    flake8 vcd_plugin_sdk cloudify_vcd vcloud_server_plugin vcloud_network_plugin

[testenv:unit_tests]
commands =
    pytest vcd_plugin_sdk cloudify_vcd vcloud_server_plugin vcloud_network_plugin
